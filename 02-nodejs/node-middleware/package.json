{
  "name": "middleware",
  "version": "1.0.0",
  "description": "A basic Express server with one middleware function (`logger`) that runs before every request reaches its route handler.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
